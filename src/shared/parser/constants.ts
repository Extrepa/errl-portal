/**
 * Regular expression patterns for parsing SVG attributes
 */

/**
 * Matches numbers (integers and floats, positive and negative)
 * Examples: 1, -1, 1.5, -1.5, .5, -.5
 */
export const reNum = '-?\\d*\\.?\\d+';

