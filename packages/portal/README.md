# Portal Package

Purpose: main Errl landing experience (router, navigation, portal pages).

- Source goes in `src/`
- Builds output to `out/portal`
- Depends on shared fx/assets via `@shared/*`

TODO: move existing portal source here during Stage 3.
