### 🧮 Studio — Math Lab

A playground of **100 interactive mathematical effects** (Canvas, SVG, CSS).

**Run locally**
```bash
npm run dev
# → http://localhost:5173/studio/math-lab/
```

**Features**
- Live sliders & parameter controls
- Real-time canvas/SVG updates
- Code snippets shown per effect
- Neon dark mode interface
- 100+ visual “math toys” fully functional

**Dev testing**
```bash
npm i -D @playwright/test
npx playwright install
npx playwright test
```

**Next Enhancements**
- Export PNG/SVG
- Preset save/load system
- Category filters
- FPS overlay (dev-only)
